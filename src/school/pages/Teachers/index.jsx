import React from 'react';
import TeachersList from './TeachersList';
import TeachersGrid from './TeachersGrid';

export { TeachersList, TeachersGrid };

const Teachers = () => {
    return <TeachersList />;
};

export default Teachers;
